namespace World
{
    public sealed class UnitGenerator
    {
        private readonly WorldModel _model;

        public UnitGenerator(WorldModel model)
        {
            _model = model;
        }

        private void AddUnit(int teamId)
        {
            
        }
    }
}